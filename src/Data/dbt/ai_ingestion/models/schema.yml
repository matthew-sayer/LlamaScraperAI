
version: 2

models:
  - name: standard_transformations
    description: "Set up your transformations in here, or create new models."
    columns:
      - name: id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null